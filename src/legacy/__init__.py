"""Legacy modules from the Classic analysis pipeline.

These modules are retained for backward compatibility with non-hybrid
analysis paths.  New code should use the active pipeline modules
(``src.domain_clustering``, ``src.preprocess_fft_safe``,
``src.ring_analysis``, etc.) instead.
"""
